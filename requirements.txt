typer[all]>=0.9.0
rich>=13.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
pyngrok>=7.0.0

# Dependencies from py-cloud-connector (required even if installed as editable)
pydantic>=2.0.0
boto3>=1.34.0
cryptography>=42.0.0
python-dateutil>=2.8.0

# Local dependencies (must be installed separately if using monorepo structure)
# Uncomment if you have these libraries available in ../libs/
# -e ../libs/py-cloud-connector
# -e ../libs/py-normalize
#
# Note: If these libraries are not available, the CLI will work in limited mode.
# Token management and some advanced features require these dependencies.
